environment: prod

api:
  replicaCount: 3
  appEnv: prod
  resources:
    requests:
      cpu: 200m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

web:
  replicaCount: 2

ingress:
  enabled: true
  host: app.fullstack.local

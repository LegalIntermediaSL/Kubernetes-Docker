apiVersion: v1
kind: Service
metadata:
  name: {{ include "fullstack-demo.fullname" . }}-redis
  labels:
{{ include "fullstack-demo.labels" . | indent 4 }}
spec:
  selector:
    app: {{ include "fullstack-demo.fullname" . }}-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP

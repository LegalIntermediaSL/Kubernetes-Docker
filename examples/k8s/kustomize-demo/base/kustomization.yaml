apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: python-api-config
    literals:
      - APP_NAME=Python API Kustomize
      - APP_ENV=base
      - PORT=8000

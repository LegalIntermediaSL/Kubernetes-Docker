apiVersion: v1
kind: Pod
metadata:
  name: rbac-reader
  namespace: rbac-demo
spec:
  serviceAccountName: rbac-reader
  containers:
    - name: shell
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]

apiVersion: v1
kind: Service
metadata:
  name: python-api-keda
  namespace: keda-demo
  labels:
    app: python-api-keda
spec:
  selector:
    app: python-api-keda
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP

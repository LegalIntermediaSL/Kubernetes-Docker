apiVersion: v1
kind: Service
metadata:
  name: python-api
  namespace: cert-manager-demo
  labels:
    app: python-api
spec:
  selector:
    app: python-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP

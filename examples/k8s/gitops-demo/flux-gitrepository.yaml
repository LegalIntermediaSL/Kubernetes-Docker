apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: course-repo
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/example/Kubernetes-Docker.git
  ref:
    branch: main

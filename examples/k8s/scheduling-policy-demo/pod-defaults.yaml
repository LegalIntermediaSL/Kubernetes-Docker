apiVersion: v1
kind: Pod
metadata:
  name: defaults-demo
  namespace: scheduling-demo
spec:
  restartPolicy: Never
  containers:
    - name: defaults-demo
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]

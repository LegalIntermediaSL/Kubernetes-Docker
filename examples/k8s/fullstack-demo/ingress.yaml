apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fullstack-demo
spec:
  rules:
    - host: fullstack.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 80

apiVersion: v1
kind: Service
metadata:
  name: rollouts-bluegreen-active
  namespace: rollouts-demo
spec:
  selector:
    app: rollouts-bluegreen-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

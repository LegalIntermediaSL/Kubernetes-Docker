apiVersion: v1
kind: Service
metadata:
  name: rollouts-canary-service
  namespace: rollouts-demo
spec:
  selector:
    app: rollouts-canary-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

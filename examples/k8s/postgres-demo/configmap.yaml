apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-demo-init
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS demo_items (
      id SERIAL PRIMARY KEY,
      name TEXT NOT NULL
    );

    INSERT INTO demo_items (name)
    VALUES ('docker'), ('kubernetes'), ('postgres')
    ON CONFLICT DO NOTHING;

apiVersion: v1
kind: Service
metadata:
  name: python-api-v1
  namespace: gateway-demo
spec:
  selector:
    app: python-api-v1
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP

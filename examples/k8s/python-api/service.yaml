apiVersion: v1
kind: Service
metadata:
  name: python-api
  namespace: python-api-demo
spec:
  selector:
    app: python-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
